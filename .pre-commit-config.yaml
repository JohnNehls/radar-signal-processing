    repos:
    -   repo: https://github.com/astral-sh/ruff-pre-commit
        # Find the latest tag on GitHub.
        rev: v0.15.8
        hooks:
            # Run the linter.
            -   id: ruff
                args: [--fix]
            # Run the formatter.
            -   id: ruff-format
